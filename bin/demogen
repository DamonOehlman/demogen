#!/usr/bin/env node

var path = require('path');
	
require('scaffolder').create(path.resolve(__dirname, '../'), function(generator, action) {
	(action || generator.build).call(generator, function() {
		// done
	});
});